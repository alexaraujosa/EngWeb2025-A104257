extends layout 

block content 
    .w3-card-4 
        header.w3-container.w3-teal 
            h1 Edição do Aluno
    .w3-container.w3-teal
        form.w3-container(method="POST").w3-teal
            fieldset 
                label GITLINK 
                input.w3-input.w3-round(type="text" name="gitlink" value=aluno.gitlink)
            fieldset 
                legend TPC 
                each num in Array.from({length: 9}, (_, i) => i)
                    if aluno["tpc" + num]
                        input.w3-check(type="checkbox" name="tpc" + num value="1" checked)
                    else 
                        input.w3-check(type="checkbox" name="tpc" + num value="1")
                    label TPC #{num} 
            button.w3-btn.w3-green.w3-mb-2(type="submit") Update
        h1 
            a(href="/alunos") Voltar 
        footer.w3-container.w3-teal 
            adress Generated by EW2025 class at #{date} 